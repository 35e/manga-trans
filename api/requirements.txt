# Detection runs comic-text-detector through OpenCV's ONNX backend, so no torch
# and no onnxruntime are needed. The weights are not on PyPI; mangatrans.detect
# fetches them on first use.
opencv-python-headless>=4.8
numpy>=1.26
# 10.1 for ImageFont.load_default(size=...), used when no system font is found.
pillow>=10.1
# The API. 3.1 for MAX_FORM_MEMORY_SIZE, which bounds the JSON sent beside the
# image; MAX_CONTENT_LENGTH bounds the image itself.
flask>=3.1
# A real WSGI server: Flask's own is for development only.
waitress>=3.0
# Reading Japanese. manga-ocr is trained on manga, so it copes with vertical
# lines and stylised fonts that general OCR does not. It brings torch and
# transformers with it: the one heavy dependency here, and the reason the image
# is a few gigabytes rather than a few hundred megabytes. The Dockerfile
# installs torch from PyTorch's CPU index first so pip does not pull CUDA in.
manga-ocr>=0.1.14
# Reading everything else: PP-OCR's recognition models, which are a few
# megabytes per language, through RapidOCR on onnxruntime. 3.9 for the
# `params={"Rec.lang_type": ...}` form used in mangatrans.read.
#
# It asks for opencv-python — the same cv2 as the headless build above, but
# wanting a libGL no server needs. The Dockerfile puts the headless one back over
# it; installing this by hand on a desktop, either will do.
rapidocr>=3.9
onnxruntime>=1.17
