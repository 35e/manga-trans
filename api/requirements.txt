# Detection runs comic-text-detector through OpenCV's ONNX backend, so no torch
# and no onnxruntime are needed. The weights are not on PyPI; mangatrans.detect
# fetches them on first use.
opencv-python-headless>=4.8
numpy>=1.26
# 10.1 for ImageFont.load_default(size=...), used when no system font is found.
pillow>=10.1
# The API. 3.1 for MAX_FORM_MEMORY_SIZE, which bounds the JSON sent beside the
# image; MAX_CONTENT_LENGTH bounds the image itself.
flask>=3.1
# A real WSGI server: Flask's own is for development only.
waitress>=3.0
# Reading the lettering. manga-ocr is trained on manga, so it copes with
# vertical lines and stylised fonts that general OCR does not. It brings torch
# and transformers with it: the one heavy dependency here, and the reason the
# image is a few gigabytes rather than a few hundred megabytes. The Dockerfile
# installs torch from PyTorch's CPU index first so pip does not pull CUDA in.
manga-ocr>=0.1.14
