# Detection runs comic-text-detector through OpenCV's ONNX backend, so no torch
# and no onnxruntime are needed for it. The weights are not on PyPI;
# scripts/fetch_models.py fetches them.
opencv-python-headless>=4.8
numpy>=1.26
# 10.1 for ImageFont.load_default(size=...), used when no system font is found.
pillow>=10.1
# The review GUI.
flask>=3.0
# Recognition. 0.1.16 is the first release that works with transformers >= 5.13.
# This is what pulls in torch; everything else here is small.
manga-ocr>=0.1.16
